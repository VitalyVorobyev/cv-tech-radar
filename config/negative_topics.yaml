negative_topics:
  - face recognition
  - person re-identification
  - fashion retrieval
  - medical segmentation
  - remote sensing
  - diffusion art generation
  - video generation
  - image generation
  # MLLM/VLM-evaluation noise — added after 2026-05-11 and 2026-05-08 curation
  # showed 14 false positives in 50 candidates. Phrases are chosen to fire on
  # MLLM-eval papers without breaking legitimate vision-foundation-model work
  # (DINOv2 / SAM / CLIP papers don't use these phrases verbatim).
  - vision-language
  - multimodal large language
  - image editing
  - omnimodal
  # Autonomous-driving noise — item 354 (CARD) slipped into Use on the seed-script
  # path with no negative penalty despite an abstract that opens with
  # "Autonomous driving must operate...". The radar is industrial CV focused
  # (calibration, feature detection, segmentation, inspection) — cars/lidars
  # for AD are an unrelated stack. `lidar` stays a positive keyword because
  # it's a legitimate industrial 3D sensor.
  - autonomous driving
  - self-driving
  # Video-deflickering noise — 2026-05-20 item 1766 (VDFP) floated into the top
  # 25 and matched the Calibration & Camera Models + Sensors tracks purely
  # because screen-capture flicker-banding papers mention "rolling shutter".
  # `rolling shutter` stays a positive keyword (it's a real calibration/sensor
  # topic); `deflickering` is specific to the video-restoration task and does
  # not collide with any radar track.
  - deflickering
  # Medical-imaging noise — 2026-05-21 curation queued 5 medical papers into 25
  # candidate slots (VEELA CTA vessel segmentation, OSS surgical-skill
  # assessment, case-aware medical-image classification, two frozen-VFM/VLM
  # medical benchmarks). `medical segmentation` above was too narrow: those
  # abstracts say "computed tomography", "surgical", "medical imaging" instead.
  # The radar is industrial-CV focused (calibration, inspection, 3D sensors,
  # tracking, OSS tooling); medical imaging is an unrelated domain and these
  # phrases are medical-specific enough not to collide with any radar track.
  - medical imaging
  # `computed tomography` is KEPT but guarded — see the `exemptions:` block.
  # Re-probed 2026-08-18 against 1981 curated items it scored 14 hits at a 21%
  # keep rate — dead on the 20.3% base rate — and all three kept hits are the
  # radar's own non-destructive inspection work: 1348 (Defect Detection in
  # X-ray Tomography of Aerospace Composites), 7081 (XCT-SAM, industrial CT
  # domain adaptation), 9260 (Metal Artifact Reduction in Cone-Beam CT).
  # Industrial XCT is a core inspection modality, so the bare phrase was
  # demoting the domain it was meant to protect. That round concluded by
  # deleting the phrase; the exemption mechanism landed separately and is the
  # better instrument — the industrial guards spare all three kept items while
  # the phrase keeps demoting genuinely medical CT, which deletion gave up on.
  # `cone-beam ct` was rejected as a replacement — it hits 9260.
  - computed tomography
  - chest ct
  - pet/ct
  - low-dose ct
  - liver
  # `clinically` added 2026-08-18: `clinical` has been listed since May and the
  # matcher is whole-word, so "Clinically-Constrained" never fired. 29 hits,
  # 0 kept. Third instance of this morphological miss in one round, after
  # radiology/radiological and photogrammetry/photogrammetric.
  - clinically
  - histopathology
  - surgical
  - clinical
  # Image-restoration / super-resolution / T2I-decoder noise — 2026-05-22
  # queued three restoration/generation papers (SANA-SR one-step diffusion SR,
  # PixIE low-light enhancement, PiD pixel-diffusion text-to-image decoder)
  # that piggy-backed on `dinov2`, `foundation model`, `reconstruction`,
  # `pruning`, and `deployment` matches. Generic image restoration / SR /
  # enhancement / T2I are outside the radar (we already filter `image editing`,
  # `image generation`, `video generation`); these phrases close the obvious
  # remaining gap without colliding with any positive radar keyword.
  - super-resolution
  - image restoration
  - low-light image enhancement
  - text-to-image
  # Deepfake / synthetic-image forensics noise — the 2026-07-08 backfill
  # (07-03..07-07) surfaced three forensics papers (VendorBench-100 deepfake
  # benchmark, "Generalized Synthetic Image Detection", XPlainVerse explainable
  # deepfake detection) that piggy-backed on `benchmark` / `dataset` / `github`
  # matches and one even floated to a suggested Watch. Media-forensics / deepfake
  # detection is outside the industrial-CV radar; these phrases are specific to
  # the forensics task and do not collide with `synthetic data` (the Synthetic
  # Data & Simulation track uses the two-word phrase) or any other radar keyword.
  - deepfake
  - synthetic image detection
  # Medical-image-segmentation / ultrasound noise — the 2026-07-08 daily run
  # queued four medical papers into 25 candidate slots that carried *zero*
  # negative penalty (HPR-SAM prompt-free medical seg, TRACE-Seg3D 3D glioma
  # seg, EP-SAM ultrasound seg, SonoRank forearm-ultrasound prosthetics). They
  # piggy-backed on `sam` / `benchmark` / `dataset` / `github` matches. The
  # earlier `medical imaging` / `medical segmentation` / `clinical` / `surgical`
  # negatives missed them: those abstracts say "medical image segmentation"
  # (≠ "medical imaging") and "ultrasound". Both phrases are medical-specific
  # and do not collide with any radar keyword — `medical image segmentation`
  # is a full phrase (industrial work says defect/anomaly/surface segmentation)
  # and no positive keyword contains `ultrasound` (whole-word matched, so it
  # will not fire inside another token).
  #
  # 2026-08-05: `medical image segmentation` (here) and `medical image analysis`
  # (below, in the 2026-07-24 block) were both folded into the bare
  # `medical image`, which strictly subsumes them under whole-word matching and
  # stops the two of them stacking a redundant +10 on the same abstract. The
  # bare form is 23 Ignore / 0 kept over the 1652-item decided corpus, 5 of
  # those Ignores carrying no penalty at all today — 8261 (recurrent contrastive
  # learning for imbalanced *medical image classification*) is this round's
  # instance, which reached the queue on `github` alone. `medical imaging` stays
  # a separate entry: it is NOT subsumed (whole-word, "image" ≠ "imaging").
  # `medical images` is listed for the same reason — the plural is its own
  # phrase and 8 Ignore / 0 kept.
  # Bare `medical` remains deliberately unused: it fires inside "biomedical" and
  # on kept items that name medicine as one application domain among many.
  - medical image
  - medical images
  - ultrasound
  # Vehicle re-identification noise — the 2026-07-09 daily run queued item 6227
  # (EV-MoE multi-query *vehicle* ReID + the LCRI-1K benchmark) at rank 3
  # (final 41.8) with a *false* 3D Geometry & Reconstruction track match: its
  # `multi-view` / `reconstruction` hits are ReID feature-fusion terms, not 3D
  # geometry. No ReID keyword fired — the abstract uses the "ReID" abbreviation,
  # so both the existing `person re-identification` negative and the
  # `re-identification` Object-Tracking positive keyword missed it. Vehicle /
  # ITS re-identification is surveillance retrieval, outside the industrial-CV
  # radar. Scope is deliberately vehicle-specific so it does NOT penalise
  # legitimate multi-object-tracking papers that use ReID as an association
  # component — the Object Tracking track keeps bare `re-identification` as a
  # positive keyword by design (see topics.yaml). Both phrases are whole-word
  # matched and collide with no positive radar keyword.
  - vehicle re-identification
  - vehicle reid
  # Human-centric 3D / pose / mesh / motion noise — the 2026-07-10 daily run
  # queued three human-body papers into the top-25 with *zero* negative penalty
  # (TSR-Ego egocentric 3D human pose, DETRAM end-to-end human mesh recovery +
  # tracking, and 4D Human-Scene reconstruction). They leak through the 3D
  # Geometry & Reconstruction / Object Tracking tracks via `pose estimation` /
  # `stereo` / `fisheye` / `reconstruction` / `multi-view` / `tracking`. Human
  # pose/mesh/motion is a *recurring* class — human-centric titles show up in
  # 5–9 candidate slots per recent batch (07-02..07-09), e.g. the 07-09 ARDY
  # "Interactive Human Motion Generation". Human-body CV (HPE / HMR / motion
  # generation / egocentric wearable vision) is outside the industrial-CV radar.
  # Scope is deliberately the two-word `human <x>` phrase (NOT bare `pose`):
  # `pose estimation` / `pose refinement` / `motion model` stay POSITIVE
  # calibration keywords, and a camera-pose paper never says "human pose". The
  # phrases avoid the existing "human performance" MMVIAD guard fixture (that is
  # "human-level performance", not a human-body topic). `egocentric` is a clean
  # first-person / wearable-AR marker that collides with no radar keyword (and
  # does not fire on "egomotion", a different token).
  # `human pose` was added by the 2026-07-27..08-06 line and rejected 2026-08-24 alongside `hand pose`: both touch kept items.
  # Re-probed against the larger corpus, the rejection wins.
  - human mesh
  - human motion
  # `egocentric` was added by the 2026-07-27..08-06 line and rejected 2026-08-14 and re-confirmed 08-19: hits 8 kept items.
  # Re-probed against the larger corpus, the rejection wins.
  # Video-LLM / video-QA evaluation noise — the 2026-07-13 daily run queued FIVE
  # video-language papers into the top-25, three of them pure video-LLM
  # benchmarks with *zero* negative penalty (6395 "Do Video-LLMs Actually Watch?"
  # character-tracking diagnostic, 6323 Evidence-Backed Video Question Answering,
  # 6367 SLVMBench skill-learning-from-video). They piggy-back on `benchmark` /
  # `dataset` / `github` / `open source` / `toolkit` matches, and two float in on
  # *false* Object Tracking hits ("character-tracking", not MOT). Video-LLM
  # evaluation is a *recurring* class — video-language titles recur across recent
  # batches (07-01..07-07 carried 3+3+2+1 such candidates). Long-video-under-
  # standing / video-QA is outside the industrial-CV radar. Scope drops the
  # trailing word ("model") so both singular and plural match under the
  # `\b`-anchored whole-phrase matcher (same reason `multimodal large language`
  # is written without "model"): `video large language` catches "Video Large
  # Language Model(s)" and `video question answering` catches the VQA genre.
  # Neither collides with any positive radar keyword — genuine video object
  # detection / MOT papers do not use these phrases, and the Object Tracking
  # track keeps bare `tracking` / `object tracking` positives intact.
  - video large language
  # 2026-07-27: `video question answering` widened to bare `question answering`.
  # Mined over the 1125-item decided corpus, `question answering` hits 35 Ignore
  # items and 0 kept ones, and it strictly subsumes the narrower phrase (all 5
  # video-QA items also match the short form), so widening adds 30 items without
  # double-counting the penalty on the ones already caught. The VQA/3D-QA genre
  # is uniformly off-radar: it evaluates language grounding, not measurement.
  - question answering
  # Medical / healthcare-training noise — the 2026-07-13 daily run candidate 6325
  # (LoRA cascaded multimodal fusion for action recognition in *healthcare*-
  # oriented / nurse-training environments; NurViD + Nurse Training datasets)
  # slipped into the top-25 with *zero* negative penalty. The existing medical
  # negatives (`medical imaging` / `medical image segmentation` / `clinical` /
  # `surgical` / `computed tomography`) all missed it: the abstract frames the
  # domain as "healthcare-oriented training environments", not medical *imaging*.
  # `healthcare` is a clean off-radar domain marker (whole-word matched) that
  # collides with no radar keyword — industrial-CV abstracts (calibration,
  # inspection, 3D sensors, tracking, OSS tooling) do not use it. Extends the
  # recurring medical-noise class documented in the medical blocks above.
  - healthcare
  # Dermatology / computational-pathology noise — the 2026-07-14 daily run
  # queued FIVE medical papers into the top-25 (the recurring #1 Ignore class),
  # two of them reaching the queue with *zero* negative penalty because the
  # existing medical negatives cover neither sub-domain:
  #   * candidate 8 (6425 cgDDI — controllable *dermatological* imagery
  #     generation for malignancy classification) had zero penalty, and
  #     candidate 1 (6423 DermDepth — monocular metric-scale 3D reconstruction
  #     for *dermatology*) floated to RANK 1 (final 42.45) on only the `clinical`
  #     hit. Nothing matched the skin/dermatology domain itself.
  #   * candidate 12 (6469 CGRL — *computational pathology* / *whole-slide image*
  #     classification, TCGA-BRCA/NSCLC) had zero penalty: the existing
  #     `histopathology` negative did not fire because the abstract says
  #     "computational pathology" and "whole-slide image", not "histopathology".
  # Dermatology (skin-lesion / ISIC-style) and WSI pathology (TCGA) are two of
  # the most common medical-CV sub-fields and will recur. All four phrases are
  # medical-specific, whole-word matched, and collide with no positive radar
  # keyword (no calibration / inspection / 3D / tracking / OSS term contains
  # `dermatolog*`, `pathology`, or `whole-slide`). Kept as scoped phrases rather
  # than a blunt bare `medical` to avoid penalising on-radar papers that mention
  # "medical" in passing — though the medical tail keeps growing, so bare
  # `medical` remains the consolidation option if it persists.
  # `whole-slide image` was superseded on 2026-07-30 by the bare `whole-slide`
  # below: the matcher is whole-word, so the longer form missed 7621, whose
  # abstract says "whole-slide images". Keeping both would also double-count into
  # the penalty, so only the short form survives.
  #
  # `computational pathology` was likewise superseded on 2026-07-31 by the bare
  # `pathology` in the batch below — 7829 says "pathology foundation models", not
  # "computational pathology". Same rule as above: the longer form is strictly
  # subsumed, and keeping both would double-count the penalty on the two items
  # (4488, 6469) that use the two-word phrase.
  - dermatology
  - dermatological
  # Wildlife / ecology / agronomy noise — the 2026-07-22 backfill (07-15..07-21,
  # 557 items, 7 candidate queues) made this the single largest *uncovered* noise
  # class: NINE off-radar ecology/agriculture papers reached the top-25 with zero
  # negative penalty, two of them at RANK 2 of their day (6707 tomato phenotyping
  # via procedural synthetic data, 6655 Delineate Anything v2 global field
  # delineation). The rest: 6745 NACTI camera-trap species recognition, 6887
  # AnimalCLEF multi-species animal ReID, 6808 leprosy in wild chimpanzees, 6807
  # PanAf-SBR great-ape social behaviour, 7067 multi-species plant recognition,
  # 7019 tomato detection/segmentation/tracking, 6696 marine species recognition.
  # They piggy-back on `benchmark` / `dataset` / `github` / `re-identification` /
  # `tracking` matches. All seven phrases were probed against the full 557-item
  # window: they hit only items decided Ignore and collide with nothing kept.
  # Bare `agricultural` (6 hits, all off-radar) is deliberately NOT added —
  # agri-robotics is adjacent to the Robotics Vision / robot-guidance track, and
  # `phenotyping` + `cropland` already cover the plant-CV and geospatial cases.
  - wildlife
  - camera trap
  - species recognition
  - biodiversity
  - phenotyping
  - marine species
  - cropland
  # Affect / emotion recognition noise — four papers in the same window (6933
  # multimodal ambivalence and hesitancy, 6733 SpEmoC speaker-segment emotion
  # benchmark, 6860 facial action unit detection for driver monitoring, 6873
  # TellTale ambivalence) plus a cluster of ABAW-competition entries (6531, 6882,
  # 7044), all at zero penalty. Affective computing is outside the industrial-CV
  # radar. `affective` is a clean domain adjective that appears in no radar
  # keyword.
  #
  # 2026-07-23: `facial expression` and `facial action unit` were consolidated
  # into bare `facial`, and `gaze` was added. Three more human-face papers leaked
  # at zero penalty in the 07-21..07-22 window, one of them at RANK 3 — 6681
  # (UVFaceFusion, fixed-topology *face* reconstruction for digital avatars,
  # final 40.9), 7179 (ReFace, pain assessment from *facial* video) and 6678
  # (open-vocabulary *gaze* object prediction). None of them says "facial
  # expression" or "facial action unit"; they say "facial geometry", "facial
  # video" and "gaze". Probed over 07-01..07-22 (1699 items): `facial` hits 40
  # items and `gaze` 13, and NONE of the 75 items the curator kept. Consolidating
  # also removes a double-count — an ABAW paper used to match two phrases for one
  # underlying signal.
  - emotion recognition
  - facial
  # `gaze` was added by the 2026-07-27..08-06 line and rejected 2026-08-18 as the broad form; `gaze estimation` / `gaze prediction` are the scoped forms kept instead.
  # Re-probed against the larger corpus, the rejection wins.
  - affective
  # Document / handwriting OCR noise — 6557 Devanagari handwriting recognition,
  # 6917 handwritten vs printed text segmentation, 7000 newspaper layout
  # structure, 6970 cross-lingual handwritten OCR, all zero-penalty in the same
  # window. Document analysis is off-radar. `document layout` is deliberately
  # omitted: it also fires on AEC engineering-drawing layout detection (6665),
  # which is plausibly on-radar for CAD/industrial workflows.
  - handwritten
  - optical character recognition
  # Medical sub-domains still uncovered after the dermatology/pathology round —
  # the same window queued 6983 echocardiography, 6974 screening mammography and
  # 6699 breast-MRI classification into the top-25 at zero penalty, because the
  # existing medical negatives name neither the modality nor these organs. Bare
  # `mri` is whole-word matched and hit 15 items in the window, every one of them
  # medical and none of them kept — no industrial-CV abstract uses the token.
  # `magnetic resonance` is listed separately because the spelled-out form
  # appears in abstracts that never use the abbreviation.
  #
  # 2026-07-23: `magnetic resonance imaging` was shortened to `magnetic
  # resonance`, and `clinically` was added. 7138 (a cardiac-disease diagnosis
  # pipeline over *cardiovascular magnetic resonance*) reached RANK 2 of its day
  # at final 41.1 with ZERO penalty: the abstract says "cardiovascular magnetic
  # resonance" (never "…imaging") and "clinically meaningful" (the `clinical`
  # negative is whole-word matched, so it does not fire on the adverb). Probed
  # over 07-01..07-22: `magnetic resonance` hits 21 items and `clinically` 48,
  # none of them kept. Medical imaging remains the #1 Ignore class.
  # (`clinically` itself is listed once in the medical block above — the
  # 2026-08-18 round rediscovered it independently. Duplicating a phrase is not
  # inert: the penalty is len(matches)-driven, so it would charge 35 points
  # instead of 25.)
  - mri
  - magnetic resonance
  - echocardiography
  - mammography
  # Geospatial / satellite mapping noise — the 07-21..07-22 window queued four
  # such papers at zero penalty (7143 seasonal Sentinel-2 building detection,
  # 7173 an accuracy audit of global building-footprint rasters, 7147
  # OffNadirLoc UAV-to-satellite geo-localisation, plus 7127 urban housing
  # survey). The existing `remote sensing` negative misses all of them: their
  # abstracts say "satellite imagery", "Sentinel-2" or "geo-spatial" instead.
  # Cross-view geo-localisation against georeferenced maps is retrieval over
  # aerial imagery, not the radar's 3D-measurement domain.
  #
  # Probed over 07-01..07-22, these four phrases hit exactly ONE kept item: 6669
  # (NGPS, GPS-denied aerial positioning, 27.2 -> 23.4). That hit is accepted
  # rather than guarded — NGPS earned its Watch on the *sensor-fusion design*
  # (RANSAC-inlier-weighted UKF covariance), while its domain genuinely is
  # satellite-image matching, and the recorded decision already overrides the
  # score. Bare `urban` (22 hits, one of them a Prototype item) and `cross-view`
  # (29 hits, three kept) were both probed and deliberately NOT added.
  # `satellite imagery` was added by the 2026-07-27..08-06 line and rejected 2026-08-18: the whole `satellite*` / `geo-localization` family hits kept 6669 (NGPS); `land cover` is taken instead.
  # Re-probed against the larger corpus, the rejection wins.
  - sentinel-2
  - geospatial
  - building footprint
  # Biometric identity / authentication noise — the 2026-07-23 queue held THREE
  # such papers at zero penalty (7283 MAGE-Vein finger-vein age/gender, 7212
  # occlusion-aware iris recognition, 7261 text-based person retrieval), and a
  # fourth (7282 face anti-spoofing) missed the top-25 only narrowly. The two
  # existing phrases cover the class far too narrowly: `face recognition` is one
  # modality and `person re-identification` misses the "person retrieval"
  # phrasing. Identity verification is a distinct field from the industrial-CV
  # radar — the modalities (iris, vein, periocular, gait, signature) share no
  # vocabulary with calibration, inspection, 3D or tracking work.
  #
  # Probed over the full 7299-item corpus: the ten phrases below hit 58 items and
  # NOT ONE of the 249 items the curator kept. Deliberately omitted:
  #   * bare `fingerprint` (29 hits) — polysemous. It is the standard term for
  #     AI-generated-image attribution, behavioural telemetry, model watermarking
  #     and thermal side channels. `biometric`/`biometrics` already catches the
  #     genuine fingerprint-biometrics papers (4023, 4476, 3871).
  #   * `palmprint` and `vein recognition` — zero marginal coverage; every item
  #     they hit is already caught, so they would only double-count the penalty.
  #   * bare `iris`, `gait`, `signature`, `spoofing` — all collide with unrelated
  #     senses (`signature` in particular: "spectral signature").
  # `biometrics` is listed alongside `biometric` because matching is whole-word:
  # `\bbiometric\b` does not fire on "biometrics", and the plural alone uniquely
  # covers four items. Accepted cost: `gait recognition` also catches 1985
  # (EventGait), an event-camera paper — the sensor is interesting but the task
  # is surveillance biometrics, and the phrase is scoped tightly enough that
  # event-camera *tracking* or *sensing* work is untouched.
  - biometric
  - biometrics
  - iris recognition
  - periocular
  - finger vein
  - gait recognition
  - signature verification
  - presentation attack
  - anti-spoofing
  - person retrieval
  # VLM-reasoning / agent-benchmark noise — added 2026-07-27. Measured over the
  # full 1125-item decided corpus: 564 of the 1125 Ignore items (50%) still carry
  # *zero* negative penalty, and mining that uncovered residue for phrases
  # enriched against the 262 kept items surfaces one dominant coherent class —
  # papers whose contribution is a reasoning or question-answering benchmark for
  # a multimodal LLM. The existing `vision-language` / `multimodal large language`
  # negatives catch the ones that name the architecture; these catch the ones that
  # name only the *task*. Counts are Ignore/kept over the decided corpus:
  #   question answering 35/0 (above), spatial reasoning 25/0,
  #   chain-of-thought 22/0, visual reasoning 19/0,
  #   embodied agent 4/0, vision-and-language navigation 3/0.
  # Deliberately rejected after probing, all because they hit items the curator
  # *kept* — the failure mode that matters most here:
  #   * bare `embodied` (52/8) and `embodied ai` (16/4) — 6091 EmbodiedGen V2 is a
  #     kept Watch item, and 2034/4318/4638 are pose-tracking and digital-twin
  #     papers that merely cite embodied AI as motivation.
  #   * `world model` (17/2) — collides with 1144 (video world model *geometric
  #     consistency*, kept) and 6506 (GEST-Engine synthetic video).
  #   * `success rate` (19/0) — clean today, but it is a bare metric name and
  #     grasping / bin-picking work under Robot Guidance reports exactly that.
  #   * `multi-agent` (14/0) — plausible in multi-robot cell coordination.
  - spatial reasoning
  - chain-of-thought
  - visual reasoning
  - embodied agent
  - vision-and-language navigation
  # Medical tail — added 2026-07-27. Medical imaging remains the single largest
  # Ignore class and it keeps leaking one *modality* at a time: the 2026-07-24
  # queue put 7327 (CARDIAG, coronary-angiography segment classification) at rank
  # 5 with zero penalty, because none of `medical imaging` / `computed tomography`
  # / `histopathology` / `surgical` / `clinical` / `mri` name angiography. These
  # phrases are modality and clinical-entity names, each Ignore/kept-probed:
  #   patient 27/0, lesion 14/0, cardiac 12/0, tumor 10/0, radiology 8/0,
  #   angiography 4/0, fmri 3/0, eeg 3/0, biopsy 3/0, endoscopy 3/0.
  # (`ultrasound` is already listed above; it gains only an exemption guard here.)
  # Deliberately rejected:
  #   * `diagnosis` (35/1) — the highest-volume marker available, but "fault
  #     diagnosis" and "defect diagnosis" are *industrial inspection* terms. This
  #     is the failure mode the exemptions block below exists to prevent, and
  #     scoping by modality avoids it entirely.
  #   * `brain` (24/0) and `pathology` (12/0) — both fire on figurative ML usage:
  #     3447 says "modular brain-skill-runtime architecture" (the planner module)
  #     and 3987 says "action-forgetting pathology" (a defect of the method).
  #     Adjacent to Robot Guidance; `fmri` / `eeg` cover the real neuro papers.
  #   * `electron microscopy` (2/1) — 4622 (synthetic TEM generation) is kept, and
  #     SEM/TEM are semiconductor and materials *inspection* instruments.
  - patient
  - lesion
  - cardiac
  - tumor
  - radiology
  - angiography
  - fmri
  - eeg
  - biopsy
  - endoscopy
  # 2026-07-28 batch. The 07-25..07-27 weekend backfill surfaced four content
  # classes that reached the top-25 with *zero* negative penalty, each probed
  # against the 1200-Ignore / 269-kept decided corpus:
  #   * Automotive V2X — 7457 (SimBEV2X, a CARLA-based V2X dataset) took rank 2
  #     of 2026-07-27 with no penalty. The existing `autonomous driving` negative
  #     misses it: V2X abstracts say "autonomous vehicles" and "cooperative
  #     perception". v2x 7/0, cooperative perception 5/0 (only 2 overlap).
  #   * Head-avatar generation — 7430 (portrait animation), 7460 and 7473 all
  #     landed with zero penalty across three days. head avatar 4/0.
  #   * 3D content creation and virtual try-on — the generative-asset tail that
  #     `image generation` / `video generation` do not name. 3d content 5/0,
  #     try-on 4/0.
  #   * Medical tail, again — 7397 (retinal vessel segmentation) reached rank 15
  #     of 2026-07-27 with zero penalty despite saying "medical image analysis"
  #     in its first sentence. medical image analysis 5/0, vessel segmentation
  #     4/0 (one overlap).
  #   * Image coding — 7559 and 7509 carried no penalty. image compression 4/0,
  #     rate-distortion 6/0. `image compression` already exists as a per-track
  #     negative for Target Detection & Fiducials; this promotes it to a penalty.
  # Deliberately rejected this round:
  #   * `fashion` (15/0) — the highest-volume phrase available and the round's
  #     clearest figurative-register trap: 4 of its 15 hits are "in an
  #     autoregressive / end-to-end / offline fashion", and it also fires on the
  #     Fashion-MNIST dataset name. `try-on` covers the real class.
  #   * `garment` (7/0) — rejected on forward risk, not measured damage: apparel
  #     and textile defect inspection is squarely Industrial Vision Inspection.
  #   * `avatar` (8/1), `3d asset` (3/2), `asset generation` (3/2), `3d
  #     generation` (2/2) — all touch kept items (2153 real-to-twin inspection,
  #     1142 Articraft, 6091 EmbodiedGen V2, 496 Mix3R, 2005 GenRecon). Scoped to
  #     `head avatar` / `3d content` instead.
  #   * `retinal` (5/1) — hits 3103, "Programmable Silicon Retina on Pixel
  #     Processor Array", a genuine sensor paper.
  #   * `image quality assessment` (4/1) — hits 7514, kept the same day as a
  #     reference-free ISP-pipeline evaluation method.
  - v2x
  - cooperative perception
  - head avatar
  - 3d content
  - try-on
  # `medical image analysis` folded into the bare `medical image` on 2026-08-05
  # — see the note at the top of this file.
  - vessel segmentation
  - image compression
  - rate-distortion
  # 2026-07-30 batch, mined from the 07-28 and 07-29 queues (40 Ignore items, of
  # which 18 reached the top 25 carrying *zero* negative penalty). Counts are
  # Ignore/kept over the 1240-Ignore / 279-kept decided corpus, and every phrase
  # below was also eyeballed for figurative usage in its actual hits.
  #   * Medical tail, again — 7621 (anomaly detection in computational cytology)
  #     took rank 15 of 07-28 with zero penalty. cancer 13/0, whole-slide 7/0.
  #   * Scholarly-figure AI — three papers in the *same* 07-29 queue (7679
  #     SciFigQual-Bench, 7681 SciFigAlign, 7695 ICDAR ALD/E). scientific figure
  #     3/0 hits exactly those three. Document AI more broadly: document
  #     understanding 6/0, handwriting 4/0 (7707 matched 3D Geometry on the bare
  #     word "reconstruction").
  #   * Editing / saliency tasks the existing `image editing` negative misses:
  #     video editing 11/0, salient object detection 4/0 (7632 RDVSv2).
  #   * Image forensics — 7752 (BG-REAL) matched *Robotics Vision* because
  #     `manipulation` fired on "background manipulation". manipulation detection
  #     4/0, manipulation localization 3/0, image forensics 2/0. Also guarded on
  #     the robotics-vision track in topics.yaml so the relevance boost goes too.
  #   * Geospatial tail — 7602 and 7619 both landed with the geo-localization
  #     class unpenalised. cross-view geo-localization 4/0, drone-view 2/0.
  #   * Generic ML method papers riding `dataset` — dataset distillation 4/0.
  #   * Wildlife tail — taxonomic 3/0 (7705 avian bone classification; the other
  #     two hits are bird monitoring and fine-grained species).
  #   * V2X, second attempt — 7747 (HeteroPROMPT) evaded the `v2x` negative added
  #     two days ago because the matcher is whole-word and the abstract only names
  #     the *datasets*: "V2XSet" and "OPV2V-H" do not contain a standalone "v2x"
  #     token. v2xset 2/0, opv2v 2/0. Brittle by construction, but exact.
  #   * fall detection 2/0 — small but unambiguous healthcare monitoring.
  # Deliberately rejected this round, all measured:
  #   * `satellite` (28/1), `satellite image` (4/1) and `geo-localization` (7/1) —
  #     all three hit 6669 (NGPS, GPS-denied aerial geo-localization on a Jetson
  #     Orin NX), a kept Watch item whose sensor-fusion design is on-radar. This
  #     is why the geospatial negatives stay scoped to the cross-view/drone-view
  #     retrieval task rather than naming the sensor.
  #   * `collaborative perception` (3/2) — hits 4395 (HERCULES multi-robot SLAM
  #     simulator, kept Evaluate) and 7172 (DRGBT-1K, kept Watch).
  #   * `long-tailed` (7/1) — hits 717, a kept MOT paper.
  #   * `ocr` (13/0) and `text recognition` (11/1) — rejected on forward risk plus
  #     one kept hit: optical character verification on part markings and date
  #     codes is a standard machine-vision function, i.e. the radar's own domain.
  #   * `forensic` (14/0) — the highest-volume option, but the corpus already
  #     shows the figurative register ("genuine forensic logic" in 7733, a
  #     reasoning benchmark). Scoped to the three task names instead.
  #   * `eye-tracking` (5/1), `gaze` (8/1), `saliency prediction` (0/2),
  #     `bird's eye view` (16/2) — each touches a kept item.
  - cancer
  - whole-slide
  - scientific figure
  - document understanding
  - handwriting
  - video editing
  - salient object detection
  - manipulation detection
  - manipulation localization
  - image forensics
  - cross-view geo-localization
  - drone-view
  - dataset distillation
  - taxonomic
  - fall detection
  - v2xset
  - opv2v
  # 2026-07-31 batch, mined from the 07-30 queue — an unusually weak day: all 25
  # candidates were suggested Ignore, the top score was 39.7, and 12 of the 23
  # items curated Ignore carried *zero* negative penalty. Counts are Ignore/kept
  # over the 1262-Ignore / 282-kept decided corpus.
  #   * Captioning — 7779 (RefCaptioner, multi-reference video captioning) took
  #     rank 3 with no penalty, matching 3D Geometry on the bare word
  #     "reconstruction" from "video reconstruction". Bare `captioning` 19/0
  #     subsumes both `image captioning` (4/0) and `video captioning` (5/0), so
  #     only the short form is listed. Captioning is a language task throughout.
  #   * Human-body tail — the `human pose` / `human mesh` / `human motion` family
  #     added 2026-07-10 names the *task*, so it misses papers that name the
  #     subject instead: 7861 (EgoGVAE, full-body mesh from head pose) and 7872
  #     (4DHumanDiff, "360-degree dynamic humans"). full-body 6/0, dynamic human
  #     5/0. `head pose` (4/0) is clean too but strictly redundant with
  #     `full-body` on 7861, so it is omitted to avoid double-counting.
  #   * Codec tail — 7814 (ReGenVC, talking-head video codec) carried no penalty;
  #     `image compression` / `rate-distortion` (added 07-28) name the still-image
  #     task only. bitrate 5/0. Bare `codec` (5/1) and `video codec` (3/1) were
  #     both rejected: they hit 1231, a kept item that reuses AV1 motion vectors
  #     to skip exhaustive matching.
  #   * Computer-use agents — 7768 (OSReward, a reward-model benchmark for CUA
  #     trajectories) reached rank 8. computer-use 4/0; a growing cs.CV class that
  #     the VLM negatives only catch when the paper names the architecture.
  #   * Medical, again — 7829 (pathology foundation models as mitotic-figure
  #     detectors) landed at zero penalty. Bare `pathology` is 14/0 and was
  #     rejected at 12/0 on 2026-07-27 over figurative-register risk; re-audited
  #     here hit by hit, exactly ONE of the 14 is figurative ("action-forgetting
  #     pathology", 3987 — already decided Ignore, so the stray penalty is inert)
  #     and the other 13 are genuine medical pathology. Taking it, and retiring
  #     `computational pathology` which it subsumes. Medical remains the #1 Ignore
  #     class and this is the consolidation the earlier medical blocks flagged.
  # Deliberately rejected this round, all measured:
  #   * `knowledge distillation` (17/4) and bare `distillation` (73/11) — the
  #     obvious phrase for 7770 (VAD, on-policy distillation) and the round's
  #     clearest trap. Distillation is a legitimate *edge-deployment* technique
  #     here: 595 (quantization-aware distillation), 6162, 7749 (TensorRT
  #     deployment) and 6961 (geometric distillation from rectified stereo) are
  #     all kept items.
  #   * `microscopy` (11/1) and `microscopy image` (1/1) — both hit 4622, a kept
  #     synthetic-TEM-generation paper. Same reason `electron microscopy` was
  #     rejected on 07-27: SEM/TEM are materials *inspection* instruments.
  #   * `4d gaussian` (4/1) — hits 1978 (NoPo4D), kept.
  #   * `svg` (1/0), `mitotic` (1/0), `zooplankton` (1/0), `talking-head` (1/0),
  #     `body mesh` (1/0) — each covers exactly one item. Single-item phrases are
  #     not worth the list length; they are noted here so a recurrence is
  #     recognisable rather than re-mined from scratch.
  - captioning
  - full-body
  - dynamic human
  - bitrate
  - computer-use
  - pathology
  # 2026-08-03 batch, mined from the 07-31 queue (25 candidates, all suggested
  # Ignore, top score 43.4; 8 of the 25 carried zero penalty). Counts are
  # Ignore/kept over the 1283-Ignore / 291-kept decided corpus. This round the
  # n-gram enrichment over the 515 unpenalised Ignore items returned almost
  # nothing but generic English (`safety`, `constructed`, `document`,
  # `attribute`) — the confirmation that topic phrases really are near their
  # ceiling. Only the phrases below survived a hit-by-hit read.
  #   * `closed-source` 31/0 — the highest-yield clean phrase left in the corpus,
  #     and a genre tell rather than a topic: every one of the 31 is an MLLM/VLM
  #     leaderboard paper reporting "N open-source and M closed-source models".
  #     The residual risk is an open-source CV *library* release contrasting
  #     itself with proprietary tooling — none of the 291 kept items does, and
  #     such papers say "commercial"/"proprietary" instead, but this is the
  #     phrase to re-audit first if an Open-Source CV Tooling item is ever
  #     wrongly penalised.
  #   * `forensic` 16/0 — completes the forensics family (`deepfake`,
  #     `image forensics`, `manipulation detection` added earlier) by catching
  #     the papers that name the field rather than the task; 7959 (RAID) and
  #     7963 (AI-generated video attribution) are this round's instances.
  #   * `visual generation` 6/0 — 7890 (scaling of text conditioning) had zero
  #     penalty because unified-multimodal papers say "visual generation" where
  #     the existing `image generation` / `video generation` entries expect the
  #     modality named.
  #   * `hand avatar` 1/0 — single-item, taken only because it extends the
  #     established `head avatar` entry (2026-07-28) to the same class; 7893
  #     (OASIS) is the hand case.
  # Deliberately rejected this round, all measured:
  #   * `visual evidence` (28/0) — the round's second-highest yield and its
  #     clearest trap. 24 of the 28 are MLLM grounding papers, but 4 use the
  #     plain sense that belongs to *our* single-view/occlusion vocabulary —
  #     7893 says "limited visual evidence under severe self-occlusion", 6284
  #     "past visual evidence to influence the feature space". A clean kept
  #     count did not protect this one; reading the hits did.
  #   * `multi-agent` (18/0) — collides with multi-robot systems; 1738 is
  #     cooperative perception, already covered by `cooperative perception`.
  #   * `mixture-of-experts` (17/0) — an architecture, not a domain. Three of
  #     this round's Ignores were MoE papers, but MoE is equally available to
  #     foundation-model work we would keep.
  #   * `weather` (20/0) — adverse-weather robustness is a legitimate sensor
  #     concern; 989 is a LiDAR domain-shift benchmark.
  #   * `avatar` (9/1) hits 2153 (real-to-twin inspection), `medical` (64/5)
  #     hits 7917 (this round's own ZSAD Evaluate), `image quality assessment`
  #     (7/1) hits 7514 (ISP pipeline evaluation) — the recurring lesson that
  #     the broad form of a good negative lands on the radar's own domain.
  #   * `wireless` (4/0), `video compositing` (1/0), `landmine` (1/0),
  #     `path gain` (1/0) — one new off-domain class each (radio-propagation
  #     fields from point clouds, generative video compositing, demining
  #     datasets). Noted rather than listed so a recurrence is recognisable.
  - closed-source
  - forensic
  - visual generation
  - hand avatar
  # 2026-08-04 curation (08-02 + 08-03 queues, 40 Ignore in 50 candidates). The
  # round's dominant noise class — novel-view-synthesis / Gaussian-splatting
  # rendering papers, 7 of the 40 — turned out NOT to be phrase-separable and is
  # documented as such in topics.yaml under 3d-geometry-reconstruction. What was
  # separable is a spread of one-class-each off-domain tails. Measured as
  # Ignore/kept over the decided corpus (1325 Ignore / 302 kept), with the count
  # of hits that carry NO penalty today in brackets:
  #   * `audio-visual` 13/0 [11] — the round's highest fresh yield. Audio-video
  #     generation and talking-head/livestream datasets; 8176 (InteracVid) is
  #     this round's instance. Residual risk: acoustic-plus-vision sensor fusion
  #     for inspection would use this phrase, and no kept item does today — this
  #     is the first entry to re-audit if a sensor-fusion item is ever penalised.
  #   * `tool use` 16/0 [6] — agentic VLM tool-calling papers (8025, VC-Tooler).
  #     Note `agentic` itself was probed and REJECTED at 28/7: it hits the kept
  #     industrial-agent items (576 AnomalyClaw, 1832 IndusAgent, 6730 O-VAD).
  #     Those kept items say "tool-grounded"/"agent", never "tool use", which is
  #     what makes the narrow phrase safe where the broad one is not.
  #   * `action recognition` 12/0 [6] and `action understanding` 3/0 [2] —
  #     video action/activity papers; 8073 (Event ActivityNet) reached the queue
  #     on the 3D track via "action-center reconstruction".
  #   * `ai-generated image` 7/0 [3] and `ai-generated video` 3/0 [1] — extend
  #     the existing `deepfake` / `forensic` family to papers that name the
  #     artifact rather than the field; 8155, 8166 this round. Bare
  #     `ai-generated` was probed and REJECTED (21/1): it hits 2367, a kept
  #     point-cloud quality benchmark.
  #   * `image fusion` 6/0 [4] — infrared-visible fusion quality assessment
  #     (8160). Distinct from sensor fusion, which the corpus writes as
  #     "multi-modal"/"sensor fusion".
  #   * `object removal` 4/0 [3] — diffusion inpainting-and-shadow-removal
  #     (8162). Bare `inpainting` REJECTED (10/2).
  #   * `xai` 4/0 [3] — the acronym only. The spelled-out forms were all
  #     REJECTED: `interpretability` 24/1, `explainable` 12/1, `explainability`
  #     6/1, `grad-cam` 2/2, `feature attribution` 1/1 — they collide with 7547
  #     (quantization changing model reasoning) and 7982 (physics-informed steel
  #     fatigue), both kept. Interpretability is a property the radar's own items
  #     claim; only the field's acronym is safe to penalise.
  #   * `document retrieval` 3/0 [3] — completes the document-AI family started
  #     2026-07-29 (`document understanding`, `scientific figure`).
  #   * `chart understanding` 3/0 [2] — chart/plot VQA benchmarks (8156).
  #   * `astronomy` 3/0 [1] — thin but a clean, wholly off-domain class; 8016
  #     (galaxy morphology) is this round's instance. `galaxy` 2/0 and
  #     `astronomical` 1/0 left out as too thin to be worth the surface.
  # Also probed and rejected this round: `world model` 19/3 (hits 1144, 6506,
  # 7921 — all kept), `imitation learning` 6/3, `federated learning` 4/1 (hits
  # 6235, a federated *industrial* defect-detection framework), `traversability`
  # 3/1 (hits 4395 HERCULES), `fused image` 1/1.
  - audio-visual
  - tool use
  - action recognition
  - action understanding
  - ai-generated image
  - ai-generated video
  # `image fusion` was added by the 2026-07-27..08-06 line and rejected 2026-08-14: hits kept 8840 (irpol-fuse). `visible image fusion` is the scoped form kept instead.
  # Re-probed against the larger corpus, the rejection wins.
  - object removal
  - xai
  - document retrieval
  - chart understanding
  - astronomy
  # 2026-08-05 batch, mined from the 08-04 queue (25 candidates, 18 Ignore, of
  # which 6 carried *zero* negative penalty). Counts are Ignore/kept over the
  # 1652-Ignore / 309-kept decided corpus, in brackets the number of those
  # Ignores that carry no penalty at all today:
  #   * `multi-person` 7/0 [2] — extends the `human <x>` family (2026-07-17) to
  #     the multi-subject form those abstracts actually use. 8252 (3D
  #     multi-person motion prediction) is this round's instance; it reached the
  #     queue on `github` alone. Bare `motion prediction` was probed and
  #     REJECTED (2/2): it hits 4718 (SAMOFT MOT) and 6533 (Polycepta), both
  #     kept — motion prediction is what a tracker does. `pedestrian` (9/1) and
  #     `skeleton` (8/2) REJECTED for the same reason.
  #   * `scene text` 7/0 [3] — in-the-wild scene-text recognition and editing
  #     (8200 BanglaWild this round). Deliberately narrow: bare `ocr` stays out
  #     of this file, because industrial character verification is the radar's
  #     own domain and says "OCR" verbatim. `scene text` is the street-sign task
  #     and shares no vocabulary with it.
  #   * `multimodal agent` 4/0 [2] and its plural 7/0 — 8186 (Video-DeepResearch)
  #     matched only `benchmark` + `github` and took no penalty: the existing
  #     Video-LLM negatives (`video large language`, `video question answering`)
  #     miss it because the abstract abbreviates to "VQA". Bare `vqa` was probed
  #     and REJECTED (28/1) — it hits 8092 (OSMa-Bench++, a kept semantic-mapping
  #     benchmark). `coding agents` also REJECTED (1/1), hitting 1142 Articraft.
  #   * `dietary` 3/0 [2] — food/nutrition recognition (8244 OliveGemma).
  #     Thin, but a wholly off-domain class with no adjacency to any track.
  - multi-person
  - scene text
  - multimodal agent
  # The plural is the *more* common form here (7/0 vs the singular's 4/0) and is
  # what 8186 uses — "extending multimodal agents from static images". Caught on
  # a post-change re-run of the queue, where 8186 survived the singular
  # unchanged. Whole-word matching makes every plural its own phrase; probe both.
  - multimodal agents
  - dietary
  # 2026-08-06 curation. Corpus-wide counts are Ignore/kept over the decided set:
  #   * `video-language` 4/0 (29 corpus) — the hyphen variant the Video-LLM genre
  #     actually uses. `vision-language` has been a negative since 2026-05-11 but
  #     never fires on these: 8434 (CLIP-CC-Bench) says "Video-Language Models"
  #     throughout and took zero penalty. The matcher treats `-` and space alike,
  #     so this one phrase covers "video language model(s)" too.
  #   * `long-form video` 8/0 (20 corpus) — the other half of the same class:
  #     paragraph-level captioning and long-video QA benchmarks. Complements
  #     `video question answering` (2026-07-13), which misses caption-only work.
  #   * `railway` 2/0 (5 corpus) — rail perception is a small but coherent
  #     off-domain cluster; two of the 25 slots on 2026-08-05 went to it (8373
  #     GitOps annotation catalog, 8376 DB InfraGO multi-sensor dataset). Bare
  #     `rail` was not used: 3 corpus hits and it risks firing on "guard rail"
  #     and similar. Checked all 11 corpus occurrences of `railway` — every one
  #     is rail-domain, none industrial-inspection adjacent.
  - video-language
  - long-form video
  - railway

# Domain-ambiguous negatives: the phrase stays in the list above, but the penalty
# is suppressed when any guard phrase also appears in the item text. Added
# 2026-07-22 after the 07-15..07-21 backfill showed the negative filter
# penalising the radar's *core* domain — the worst failure mode available to it:
#   * 7081 XCT-SAM (defect segmentation on additive-manufacturing X-ray CT) lost
#     25 points to the medical `computed tomography` negative. Industrial XCT is
#     NDT inspection, not radiology.
#   * 6806 ("Does Super-Resolution Preserve Defect Evidence?", a low-false-call
#     semiconductor inspection benchmark) lost 25 points to `super-resolution`,
#     even though its finding — learned SR wins on SSIM while detecting fewer
#     defect pixels than bicubic — is directly actionable for inspection work.
# Both phrases stay valuable against medical and restoration noise, so they are
# scoped rather than dropped. Simulated over the full 557-item window, these
# guards exempt exactly those two items and nothing else.
  # Video-QA / spatial-reasoning / embodied-agent noise — the 2026-08-06 queue
  # had 6 of 18 Ignore items in this family (ChronoVision, GST-Bench,
  # StreamArena, TAU-Bench and friends) and 14 of 18 Ignore items carried no
  # penalty at all. Each phrase below was probed against the whole decided
  # corpus (1702 items, 320 kept) and hits ZERO kept items:
  #   question answering 40, spatial reasoning 27, embodied agents 15,
  #   video reasoning 7.
  # Deliberately NOT added, all probed and rejected because they hit kept items:
  #   `video understanding` (MMVIAD, 3 kept), `world model` (BWM simulator,
  #   3 kept), `embodied intelligence` (EmbodiedGen V2, GAUGE, 3 kept),
  #   `temporal reasoning` (multi-object-tracking active learning, 1 kept).
  - embodied agents
  - video reasoning
  # Document / handwriting AI — 2026-08-06 item 8503 (DTRNet, handwritten
  # Chinese text recognition) scored zero penalty. Both morphological forms are
  # listed because the matcher is whole-word: `handwriting` does not match
  # "handwritten". `text recognition` was rejected — it hits a kept industrial
  # visual-inspection paper.
  # Human-centric leftovers — sign-language recognition and crowd counting each
  # recur as their own small class (8459, 8461 on 2026-08-06); `activity
  # recognition` covers the RF/wireless human-activity papers (8540).
  - sign language
  - activity recognition
  - crowd counting
  # Dental imaging — a medical sub-case the existing `clinical` / `medical
  # imaging` phrases miss (8456 still ranked 15). `tooth` is deliberately NOT
  # used: gear-tooth flank inspection is the radar's own domain.
  - dental
  # Medical modality/anatomy tail — added 2026-08-11. Medical stayed the #1
  # Ignore class over the 08-07..08-10 queues (7 of the 48 unpenalised Ignore
  # items) and again leaked one modality at a time past the existing phrases:
  # ultrasound (8803), brain MRI (8790), cortical atrophy (8889), CT lung
  # nodules (8839), cross-modality translation (8807), single-cell/omics (8787,
  # 8880). Each phrase below hits ZERO kept items over 1802 curated items:
  #   ultrasound 16, lesion 15, tumor 11, radiology 9, single-cell 5,
  #   neurodegenerative 4, gene expression 2, multi-organ 2, nodule 2.
  # Measured over 49 dates: 11 decided-Ignore items pushed out, zero kept moved.
  # This finally SETTLES the standing question of consolidating to a bare
  # `medical`: probed and REJECTED — 74 hits but 6 of them kept, including 3587
  # (Comparing Commercial Depth Sensor Accuracy for Medical Applications,
  # Evaluate) and two zero-shot anomaly-detection items. Keep enumerating the
  # tail; the bare word touches the radar's own sensor and inspection work.
  # `cortical` was dropped as too thin (1 hit); `neurodegenerative` covers 8889.
  - single-cell
  - neurodegenerative
  - gene expression
  - multi-organ
  - nodule
  # Tail phrases from the 2026-08-11 queue — added 2026-08-12. This is the
  # small half of the round; negatives are near their ceiling and the batch was
  # measured at only 2 decided-Ignore items pushed out on its own. It is kept
  # because it is free: every phrase hits ZERO kept items over 1827 curated
  # items, and each one covers a class that turned up again in this queue.
  #   driving scene 6 (8983), surround-view 4 (9042), frame interpolation 4
  #   (9070), music 5 (8999), human-object interaction 5, camouflaged object 1.
  # `driving scene` and `surround-view` are the scoped forms: bare `driving` was
  # probed and REJECTED (97 hits, 11 kept — it hits SfM, SLAM and the
  # LiDAR-camera extrinsic calibration work). `scene graph` was probed and
  # REJECTED for 9012 (16 hits, 6 kept, including Hydra++ and G-MASt3R-SfM).
  - driving scene
  - surround-view
  - frame interpolation
  - music
  - human-object interaction
  - camouflaged object
  # Tail phrases from the 2026-08-12 and 2026-08-13 queues — added 2026-08-14.
  # 33 of the 50 items across those two queues were Ignore and 21 of them
  # carried no penalty at all. Each phrase below hits ZERO kept items over the
  # 1986 curated items and covers a class that showed up in this pair of
  # queues; contexts were grepped by hand for figurative usage before adding.
  # Media forensics (9273): forgery 6, deepfake 12.
  # Long-video / story understanding (9253, 9104, 9319): narrative 15,
  #   long-form video 9, cinematic 7, audio description 2.
  # Generative content tail, the class that keeps leaking past `image
  #   generation` (9248, 9224, 9171): head avatar 4, try-on 4, 3d content 5,
  #   talking head 1, human animation 1, texture generation 1.
  # Human-centric tail (9275, 9221): prosthetic 3, whole-body 9 — the latter
  #   also catches whole-body PET/CT, which is a medical sub-case.
  # One-off domains seen this round: image compression 5 (9113), volumetric
  #   video 2 (9181), business 4 (9187), plant species 1 + plant disease 2
  #   (9154), visible image fusion 4 (9276), class activation 1 +
  #   explainable artificial intelligence 3 (9106), sea ice 1 (9151),
  #   driving world 2 (9189).
  # Probed and REJECTED, all because they hit kept items: `image fusion`
  # (8840 irpol-fuse), `humanoid` (3 kept), `egocentric` (8 kept, incl. 826
  # and 803), `hand pose` (826, 803), `hand-object` (9280), `teleoperation`
  # (6332, 7588), `explainable` (8762 RobustDefect-LLM), `cultural` (8004,
  # 9245), `avatar` (2153), `species` (7749), `snapshot compressive` (1020).
  # `motion tracking` was clean (4 hits) but dropped anyway: too thin to be
  # worth blunting a phrase an industrial tracking paper could legitimately use.
  # Measured over 60 dates together with the topics.yaml changes of the same
  # date: 25 decided-Ignore items pushed out of the top-25 queues, and zero
  # kept items dropped out of any queue.
  - forgery
  - narrative
  - cinematic
  - audio description
  - talking head
  - human animation
  - texture generation
  - prosthetic
  - whole-body
  - volumetric video
  - business
  - plant species
  - plant disease
  - visible image fusion
  - class activation
  - explainable artificial intelligence
  - sea ice
  - driving world
  # Medical modality/anatomy tail, round 3 — added 2026-08-18 after the
  # 08-13..08-17 backfill window. Medical is still the #1 Ignore class and it
  # took rank 1 of the 08-17 queue with ZERO penalty: item 9348 (Unsupervised
  # Anomaly Detection for Image Dataset Quality Assurance in Multi-Center
  # Breast MRI, final 39.94) says "medical AI", "radiological" and "breast MRI"
  # and matches none of the phrases we carry. `radiology` was already listed
  # and missed it — whole-word matching again, exactly like handwriting /
  # handwritten. Keep rate over 1981 curated items (20.3% base rate); every
  # phrase below hits ZERO kept items:
  #   mri 31, patient 35, medical image 25, anatomy 24, cancer 19,
  #   pathology 19, physiological 9, radiological 5, echocardiography 5,
  #   oncology 4, dermatology 4.
  # Incremental over the existing negative list: 55 currently-unpenalised
  # decided-Ignore items gain a penalty, zero kept items move.
  # `medical image` is listed even though `medical imaging` is already here:
  # the two compile to disjoint regexes (\bimage\b does not match "imaging"),
  # and the singular form is the clean one — `medical imaging` itself hits 2
  # kept items. `radiological` is kept despite 0 incremental coverage purely as
  # the morphological guard next to `radiology`.
  # Probed and REJECTED because they hit kept items:
  #   `x-ray` (12 hits, 6 kept — X-ray tomography of aerospace defects, XCT-SAM
  #   industrial adaptation, hazelnut X-ray grading: this is the radar's own
  #   non-destructive inspection domain, the sharpest instance yet of the
  #   broad-negative-hits-industrial trap), `diagnostic` (77, 6 kept),
  #   `surgery` (10, 2), `microscopy` (14, 2), `retinal` (10, 2 — "Programmable
  #   Silicon Retina on Pixel Processor Array"), `anatomical` (47, 2),
  #   `endoscopic`/`colonoscopy` (EndoMD-SLAM), `biomedical` (TransBiolab
  #   transparent labware), `disease` (HERCULES multi-robot SLAM).
  # Dropped as pure redundancy (0 incremental over the phrases above):
  #   `prostate`, `mammography`, `carcinoma`, `breast cancer`. Bare `breast` was
  #   deliberately NOT taken even though it is 0-kept: its non-medical sense is
  #   "woody breast poultry fillets", i.e. food-quality inspection.
  - anatomy
  - physiological
  - radiological
  - oncology
  # Biometrics, presentation-attack sub-case — added 2026-08-18. The existing
  # biometrics phrases enumerate modalities (iris, vein, gait, ...) and keep
  # missing the attack-detection framing: 9573 (GBU-Palm palm PAD) and 9365
  # (ID-card PAD) both landed in the 08-14/08-17 top ten unpenalised.
  # `presentation attack` is the modality-independent phrase: 5 hits, 0 kept,
  # 5 incremental. Bare `spoofing` was not needed — every hit already carries
  # `face recognition` or a listed modality.
  # Round 2 of 2026-08-18, mined by hand from the 08-16 and 08-17 top-25 queues
  # after the medical/biometrics round above was already in place. The pattern
  # those two queues show is no longer medical: it is that a decided-Ignore item
  # reaches the top ten carrying ZERO penalty on nothing but the format tracks
  # (`dataset` / `benchmark` / `simulator`). 9439 (appliance manipulation) took
  # rank 2 and 9435 (visual dexterity in simulation) rank 4 on 08-16 that way.
  # `benchmark` itself is settled as un-removable — see the Datasets &
  # Benchmarks comment in topics.yaml — so the lever left is to name the SUBJECT
  # of each class. Keep rate measured over 1981 curated items (20.3% base rate,
  # 792 currently-unpenalised decided-Ignore items); every phrase below hits
  # ZERO kept items, and contexts were grepped by hand for figurative usage.
  #
  # Agentic / computer-use / LLM-reasoning vocabulary — the class that produced
  # 9434 (UI-Mate GUI agents) and 9439. All ten `gui` hits are GUI-agent papers;
  # `chain-of-thought` is the single largest clean phrase found this round.
  #   chain-of-thought 22 hits / 8 incremental, gui 10/9, computer-use 5/3,
  #   reward model 5/2, task planning 2/1, appliance 1/1, web agent 1/1.
  - gui
  - reward model
  - web agent
  - task planning
  - appliance
  # Human-motion generation / action recognition. `activity recognition` has
  # been listed since 08-06 and never fired on any of these: the literature
  # says "action recognition", which is a different phrase to a whole-word
  # matcher. Same morphological miss as radiology/radiological.
  #   human motion 16/11, action recognition 15/6, motion generation 8/6,
  #   human mesh 4/3.
  - motion generation
  # Affective computing / audio-visual / remote physiology. `physiological` is
  # already listed and missed every rPPG paper — they say "heart rate".
  #   audio-visual 17/11, emotion 10/6, facial expression 6/5, heart rate 5/2.
  - emotion
  - facial expression
  - heart rate
  # Media, captioning and e-commerce content generation — the tail that keeps
  # leaking past `image editing` / `image generation` (9390 GRNEdit, 9395
  # PosterText, 9397 TransAnyText).
  #   video editing 13/2, video captioning 7/3, e-commerce 5/3,
  #   3d scene generation 4/3, image captioning 4/1, sentiment 3/1,
  #   content moderation 2/1, hateful 1/1.
  - e-commerce
  - video captioning
  - image captioning
  - 3d scene generation
  - hateful
  - content moderation
  - sentiment
  # Geospatial and plant-phenotyping tail. `remote sensing` is listed but misses
  # the land-use literature and the satellite papers that never say it.
  #   land cover 11/4, phenotyping 7/5.
  - land cover
  # Biometrics tail — the modality-by-modality leak continues past
  # `face recognition` / `presentation attack`.
  #   iris recognition 2/2, gaze estimation 1/1, gaze prediction 1/1.
  - gaze estimation
  - gaze prediction
  # `gaze object` is the third scoped form: the task is named "gaze object
  # prediction"/"detection", which neither `gaze prediction` nor `gaze
  # estimation` matches under the whole-word matcher. 1 hit, 0 kept. Bare
  # `gaze` stays out — measured, it hits kept 32 (TAVIS).
  - gaze object
  #
  # Probed and REJECTED this round because they hit kept items:
  #   `satellite` (39 hits, 1 kept — 6669 NGPS aerial geo-localization; the
  #   scoped `satellite imagery`, `satellite image` and `geo-localization` all
  #   hit the same item, so the whole family stays out and `land cover` is what
  #   is taken instead), `long-horizon` (43, 5 — streaming 3D reconstruction
  #   and SLAM use it constantly), `knowledge distillation` (22, 5 — the
  #   lightweight-model work this radar wants), `visual grounding` (19, 1),
  #   `dexterous` and `dexterous manipulation` (18/13, 1 — 2858 event-based
  #   tactile force sensing), `human pose` (12, 1), `hallucination` (13, 1 —
  #   6386 GHOST hallucinates surface texture), `scene generation` unscoped
  #   (11, 1), `motion capture` (10, 1 — 8635 active-LED-marker pose solver),
  #   `pedestrian` (10, 1), `gaze` unscoped (10, 1), `food` (10, 1),
  #   `instruction following` (15, 1), `recommendation` (7, 1),
  #   `text-to-video` (6, 1), `federated learning` (5, 1 — 6235 FedTR
  #   industrial visual inspection), `aerial imagery` (4, 1), `story` (3, 2).
  # Rejected on the figurative-usage grep despite a clean kept count:
  #   `fashion` (16 hits, 0 kept, but 4 of them are "in an end-to-end /
  #   offline / autoregressive / multi-stage fashion" — the standard CV-abstract
  #   idiom, and `fashion retrieval` already covers the real class),
  #   `household` (9, 0 kept, but 2 are incidental: "50 household object
  #   categories" is a category-level 6-DoF pose dataset, i.e. YCB-style
  #   benchmarks this radar does care about; `appliance` is the scoped form
  #   taken instead), `poster` (1 hit, and "accepted as a poster" is a
  #   standing risk; `e-commerce` already covers the item).
  # Dropped as too thin to be worth blunting the word: `speaker` (2
  # incremental, and a loudspeaker is plausible sensor hardware), `ocean` (1).
  # `visual question` (21 hits, 0 kept) and `video summarization` were dropped
  # as pure redundancy — 0 incremental over `question answering`.
  # Domain-noise tail found on the 2026-08-19 tuning round, mined by hand from
  # the 157 zero-penalty Ignore items published since 08-01 (the 08-18 queue
  # alone contributed 9694 moiré removal, 9716 benthic segmentation, 9726
  # multi-modal salient object detection and 9678 agentic survey writing).
  # Format below is `phrase hits/incremental` over the 2006 decided items,
  # where incremental counts only items no existing negative already reaches.
  # Every phrase here has zero kept hits.
  #   marine 6/5, salient object 6/4, document retrieval 3/3, astronomy 3/2,
  #   document understanding 6/2, astronomical 1/1, speech 9/1.
  # `marine` is the wildlife/agronomy class reaching the water: FathomNet
  # species detection, zooplankton segmentation, benthic ecology, sonar marine
  # surveys. All 6 decided hits are Ignore. It is kept over the narrow forms
  # (`marine species`, `marine ecology`, `marine survey` — one hit each) because
  # splitting it into six single-hit phrases is brittle, but it is the one
  # phrase in this block with a known on-domain collision: 8634 (TriView-YOLO,
  # ground-penetrating-radar cavity detection) says "soft marine clay" as a soil
  # descriptor and takes the penalty for it. That item is undecided and sat at
  # rank 27 of 150 before the change and 45 after, so no queue moved — but this
  # is exactly the NDT/geotechnical adjacency the radar cares about. Drop
  # `marine` for the narrow forms if a marine-inspection item is ever kept.
  # Ship-hull and underwater weld inspection would likewise be on-domain; no
  # such item has ever been kept.
  # `salient object` is taken over `saliency` (26 hits, 5 kept — saliency maps
  # are a legitimate interpretability tool in kept work); the narrow form is
  # exactly the SOD-benchmark literature. `salient objects` adds 0 incremental
  # and is left out.
  # `document understanding` / `document retrieval` close the scholarly-figure
  # and document-AI class. Note this class was recorded as handled in an
  # earlier round but neither phrase was ever actually in this file. `ocr`
  # deliberately stays out — industrial character verification is on-domain.
  # `speech` is nearly redundant with `audio-visual` / `music` above (1
  # incremental) but is clean over 9 hits and deepens the penalty on the
  # audio-multimodal tail rather than leaving it at a single match.
  # `astronomy` + `astronomical` are listed as a pair for the same reason as
  # radiology/radiological and photogrammetry/photogrammetric: the matcher is
  # whole-word, so one form never fires on the other.
  # Rejected on kept hits this round: `underwater` (11, 1 — 6161 Wat3R
  # underwater 3D geometry), `species` (21, 1), `ecological` (8, 1),
  # `ecosystem` (6, 1), `world model` (29, 5), `video understanding` (52, 4),
  # `egocentric` (53, 7), `novel view synthesis` (51, 12 — the closed class),
  # `surveillance` (30, 4), `sports` (18, 4), `text-to-video` (6, 1),
  # `railway` (3, 1), `traversability` (4, 1).
  # Rejected on the figurative-usage grep despite a clean kept count:
  #   `sound` (5 hits, 0 kept, but "confirming the representation is sound" is
  #   the standard idiom, and `audio-visual` already covers the real class).
  # Dropped as too thin: `movie` (4, and `long-form video` / `cinematic` /
  # `audio description` already reach that class), `skeleton-based` (4, of
  # which 2 are false-context — a "skeleton-based recall loss" on road graphs
  # and a privacy-taxonomy percentage — and `action recognition` covers 9590).
  - marine
  - salient object
  - speech
  - astronomical
  # AI-media forensics — the 2026-08-19..08-21 window's #1 zero-penalty Ignore
  # class, and the one that produced the worst false-context match yet: 9849
  # (AGIDefect-4K) reached rank 2 on 08-21 because its "defects" are generative
  # artifacts in synthesised images, not manufacturing defects, so it rode
  # `defect` / `defect detection` / `defects` straight onto the Industrial
  # Vision Inspection track. 9817 (GAP-SAM manipulation localization, rank 8)
  # and 9977 (Frozen DINO image-edit localization, rank 24) are the same class.
  # `deepfake` has been listed since May and covers only the video half; the
  # still-image forensics half was entirely unpenalised. Probed against 2190
  # decided items (490 kept, 22.4% base rate), each phrase hits ZERO kept:
  #   forensic 21, forensics 9, ai-generated image 9, forgery 6,
  #   image manipulation 5, manipulation localization 5.
  # `forensic` and `forensics` are listed as a pair for the whole-word reason
  # (radiology/radiological, photogrammetry/photogrammetric, clinical/clinically).
  # Both were context-grepped: all 25 distinct hits are media authenticity, none
  # is industrial traceability or failure forensics.
  - forensics
  # (`forgery` is not repeated here — it is already carried above and a duplicate entry silently adds +10 penalty)
  - image manipulation
  # VLM token-efficiency papers — visual-token pruning/compression for LLM
  # inference cost, which is not edge CV deployment (9986 ClustRS, rank 18 on
  # 08-19, matched `deployment` + `pruning`). The PLURAL is the clean form and
  # this is a textbook [[broad-form-negative-trap]] pair: `visual token`
  # (17 hits) hits kept 8733 "Visual Token Codec", and `token pruning`
  # (17 hits) hits kept 8214 "Defect-Preserving Token Pruning" — the radar's
  # own work. `visual tokens` is 20 hits, 0 kept.
  - visual tokens
  # Omni-modal LLM assistants (9774 OmniAssistBench, rank 9 on 08-21). 5 hits,
  # 0 kept, all unambiguously Omni-LLM benchmark papers.
  - omni-modal
  # Rejected this round on kept hits: `inpainting` (22, 6 — incl. 9306, a
  # generative approach to PV-module defect classification), `quality
  # assessment` (26, 6 — incl. 9011 structured-light covariance and 9167
  # hazelnut X-ray), `hand pose` (15, 2), `human pose` (15, 1),
  # `generative model` (8, 2), `reinforcement learning` (70, 8),
  # `basketball` (4, 1 — "basketball court" as a planar scene in 5372
  # Planar-SfM), `grape` (2, 1), `underwater` (14, 1, re-confirmed).
  # Rejected on the figurative-usage grep despite a clean kept count:
  #   `manuscript` (9, 0 — but "upon acceptance of this manuscript" and "in
  #   this manuscript we release two datasets" are the standard idiom),
  #   `provenance` (7, 0 — the dataset-curation sense would hit an industrial
  #   dataset paper), `no-reference` (5, 0 — "requiring no reference photos"
  #   is a different sense), `inverse problems` (5, 0 — a legitimate term for
  #   inverse rendering and deflectometry, and 3437 is electrical capacitance
  #   tomography, which is industrial sensing).
  # Too thin to spend: `cuneiform` (2), `vector graphics` (3), `image forgery`
  # (2), `synthetic image detection` (2), `aigc` (2), `weed` (2), `vineyard` (1)
  # — all subsumed by the broader phrases above or by existing negatives.
  # -------------------------------------------------------------------------
  # 2026-08-25 round. The 08-22..08-24 queues (75 candidates, 50 Ignore) had no
  # single dominant new domain; the recurring zero-penalty residue was LLM
  # vocabulary that has no CV content at all — multimodal-LLM evaluation,
  # LLM-agent orchestration, captioning, and alignment/RLHF training papers.
  # Probed against 2156 decided items (477 kept, 22.1% base rate). Every phrase
  # below hits ZERO kept items and every distinct hit was context-grepped:
  #   multimodal models 46, captioning 29, caption 28, multi-agent 23,
  #   reward models 11, instruction tuning 14, reward model 6,
  #   preference optimization 4, sonar 5.
  - multimodal models
  - multi-agent
  - caption
  - instruction tuning
  # (`reward model` is not repeated here — already carried above; `reward modeling` below is the legitimate second form)
  - reward models
  - preference optimization
  # `sonar` closes the acoustic half of the marine class: `marine` and
  # `underwater` leave side-scan / synthetic-aperture / forward-looking sonar
  # papers unpenalised (10058, 10198 both landed in the 08-22..08-24 queues).
  # Only 5 hits, but all five are unambiguously acoustic seabed imaging.
  - sonar
  # Rejected this round on kept hits: `underwater` (17, 1 — 6161 Wat3R, the
  # THIRD time this phrase has been probed and rejected; stop re-probing it),
  # `captions` (35, 1 — hits 8798 Ego-OSCAR, a Prototype item, and the A/B
  # confirmed it drops that item from rank 2 to rank 4 on 08-08),
  # `embodied` (85, 14), `egocentric` (57, 8), `anatomical` (49, 2),
  # `facial` (39, 1), `vision-language-action` (34, 2), `world model` (31, 7),
  # `world models` (31, 4), `knowledge distillation` (23, 4), `agent` (108, 3)
  # and `agents` (91, 5) — the bare agent forms hit the radar's own
  # industrial-anomaly-detection agents (576 AnomalyCLAW, 1832 IndusAgent), so
  # `multi-agent` is the narrow form that survives — `disease` (38, 2),
  # `hallucination` (14, 2), `cifar` (12, 1), `gaze` (11, 1),
  # `light field` (4, 1), `street view` (5, 1), `quadruped` (5, 3).
  # Rejected on wrong-sense risk despite a clean kept count: `multimodal model`
  # (5, 0) — all five observed hits are the LMM sense, but "our multimodal
  # model fuses depth and intensity" is a sentence an on-domain RGB-D fusion
  # paper would plausibly write, and the plural carries 46 of the 51 hits.
  # Too thin to spend: `place solution` (3), `place technical report` (1),
  # `chemical structure` (1), `multistatic` (1), `geolocalization` (1),
  # `document classification` (1).
  # 2026-08-26 round — mined from the 08-25 queue (17 of 25 candidates were
  # Ignore) plus an n-gram sweep of the 688 currently-unpenalised decided
  # Ignore items. Probed against 2181 decided items (476 kept, 21.8% base
  # rate); every phrase below hits ZERO kept items and every distinct hit was
  # context-grepped in the full 10k-item corpus for figurative usage.
  #
  # Retrieval / RAG — retrieval and RAG papers ride `dataset` + `benchmark`
  # into the queues with no penalty at all (10338 cross-modal hashing, 10333
  # WeMM-Embedding on 08-25).
  #   image retrieval 12, retrieval-augmented 12, cross-modal retrieval 4.
  - image retrieval
  - cross-modal retrieval
  - retrieval-augmented
  # `world modeling` — the GERUND is the separable form, and this is the
  # cleanest [[broad-form-negative-trap]] instance yet found: `world model`
  # (26 hits) and `world models` (30) have each been probed and rejected twice
  # for hitting 7 and 4 kept items (BWM simulator, GAUGE physical-fidelity
  # benchmark, ContactGuard). `world modeling` hits 12 Ignore and zero kept —
  # all twelve are generative long-horizon video world models.
  - world modeling
  # `multimodal learning` — generic multimodal-fusion ML (system neuroscience,
  # urban trajectories, vision-tabular modality-missingness). 9 hits, zero
  # kept. `multi-modal learning` (1 hit) is a distinct regex under the
  # whole-word matcher but too thin to carry.
  - multimodal learning
  # Human-centric leftovers — the class is long-running (see
  # [[human-centric-noise-recurring]]); these are the phrasings the existing
  # two-word `human <x>` negatives miss. Singular and plural are separate
  # regexes under the whole-word matcher, so both `human video` and
  # `human videos` are carried, same as radiology/radiological.
  #   human videos 8, hand-object interaction 7, dynamic human 6, human video 4.
  # `hand-object` (bare) was REJECTED again: it hits kept 9280 (RGB-D handover
  # prediction) — the narrow form is what survives.
  - human videos
  - human video
  - hand-object interaction
  # Astronomy — a small but real class that the existing `astronomy` /
  # `astronomical` pair misses entirely, because sky-survey abstracts use the
  # object vocabulary instead (10317 "Decoupling candidate dual AGN from
  # chance superpositions in the GOTHIC survey" reached rank 23 on 08-25 with
  # a ZERO penalty, and it matched the Industrial track on `inspection` /
  # `visual inspection` in the astronomical sense — a [[false-context-track-
  # matches]] case). `galaxy` was REJECTED for polysemy: it hits "Samsung
  # Galaxy S21 Ultra" as the reference device of an INT8 depth-latency paper,
  # which is exactly the kind of edge-deployment work the radar wants.
  - galactic
  - galaxies
  # `authenticity` — the media-forensics class (see
  # [[ai-media-forensics-noise-class]]); 46 corpus hits, all generated-media
  # provenance, none industrial traceability. Carried partly to restore the
  # strength the duplicated `forgery` entry was supplying by accident.
  - authenticity
  # `reward modeling` — the gerund companion to `reward model` above, added for
  # the same reason: it is the legitimate second phrase for the RLHF /
  # video-reward class, replacing the accidental duplicate.
  - reward modeling
  # Rejected this round despite a clean kept count, on wrong-sense risk against
  # the radar's own domain — there is no exemption mechanism, so a phrase that
  # can fire on an on-domain paper is not worth its Ignore hits:
  #   `tampering` (3) and `forged` (17 corpus) — anti-counterfeit / tamper-
  #   evident inspection and forged metal parts are both industrial-vision
  #   subjects; `engagement` (5) — gear/tool engagement; `photometric`
  #   (18, 17 kept — photometric stereo is a core radar topic).
  # Rejected on kept hits: `hand-object` (10, 1), `text-to-video` (6, 1),
  # `human preference` (7, 1), `recommendation` (7, 1).
  # Too thin to spend: `hashing` (2), `cross-modal hashing` (1),
  # `multimodal retrieval` (3), `image forensics` (3), `telescope` (1),
  # `stellar` (2), `supermassive` (1), `dynamic humans` (1).
  # A/B over the trailing 27 days against the live config, real classifier,
  # top-25 queue rebuilt per day: 6 items pushed out of a queue (4 decided
  # Ignore, 2 undecided and off-domain), and ZERO kept items dropped.
  # Added 2026-08-28 from the 08-26 / 08-27 misfires. Each phrase was probed
  # against the whole decided corpus (2342 items, 23.1% base keep rate) and
  # kept only at zero kept-item hits.
  #
  # `transcription` (8 Ignore, 0 kept) — the document-AI / manuscript class,
  # which the existing `handwritten` / `handwriting` / `document understanding`
  # trio keeps missing: 10420 (AraMS-28k, historical Arabic manuscripts)
  # reached rank 8 of the 08-27 queue with a ZERO penalty. Bare `manuscript`
  # was REJECTED despite a clean 12/0: three of its twelve hits are the
  # meta-usage ("in this manuscript we release ..."), so an on-domain paper
  # using that phrasing would be penalised for its prose. `historical
  # manuscripts` (3) and `manuscript recognition` (2) are clean but too thin,
  # and `text recognition` is DIRTY (15/2 — it hits 6235 FedTR, industrial
  # visual inspection).
  #
  # `aesthetic` (10, 0) — aesthetic-critique / generated-image quality
  # assessment (10447 AesCanvas). The plural `aesthetics` is DIRTY: it hits
  # kept 516 (BabelDOC, layout-preserving PDF translation), so only the
  # singular is carried — the usual narrow-form-survives split.
  #
  # `dataset distillation` (5, 0) — method research with no industrial path
  # (10544 DeCO). Deliberately NOT bare `distillation`: knowledge distillation
  # to a small backbone is core Edge AI vocabulary.
  #
  # `leaf disease` (3, 0) and `plant health` (2, 0) — two holes in the agronomy
  # class: `plant disease` / `plant species` / `phenotyping` all missed 10438
  # (tea leaf disease, rank 2 on 08-27) and 10534 (CropCop plant-health, rank 6
  # on 08-26), both at a ZERO penalty. `crop disease` was REJECTED: 1 Ignore
  # against kept 4395 (HERCULES multi-robot SLAM, "crop" in the agricultural
  # robotics sense).
  #
  # A/B over 2026-08-01..08-27 against the live config, real classifier, top-25
  # rebuilt per day: 2 Ignore items pushed out and kept item 8980
  # (AlbumentationsX, Evaluate) pulled IN at rank 25. Zero kept items dropped.
  # Singular and plural are separate regexes under the whole-word matcher, so
  # both forms are carried: 10420 fires the singular ("raw diacritised
  # transcription") but its neighbours in the class only ever say
  # "transcriptions" (4 Ignore, 0 kept).
  - transcription
  - transcriptions
  - aesthetic
  - leaf disease
  - plant health
exemptions:
  computed tomography:
    - industrial
    # `manufacturing` and `aerospace` added when the 08-18 round's evidence was
    # reconciled with this guard: item 1348 (Defect Detection in X-ray
    # Tomography of Aerospace Composites) says "aerospace" and "manufacturing
    # defects" and none of the other guards, so it was still being penalised.
    - manufacturing
    - aerospace
    - additive manufacturing
    - non-destructive
    - nondestructive
    - weld
    - casting
    - semiconductor
  super-resolution:
    - defect
    - inspection
    - semiconductor
    - metrology
    - wafer
  # `ultrasound` (2026-07-27) is medical sonography, but ultrasonic NDT is a core
  # industrial inspection modality. The two normally use different words —
  # industrial abstracts say "ultrasonic testing", which the whole-word matcher
  # does not fire on — so this guard is pre-emptive rather than corrective: no
  # item in the 1125-item decided corpus is exempted by it today.
  ultrasound:
    - non-destructive
    - nondestructive
    - weld
