node_modules/
dist/
coverage/
.vite/
*.tsbuildinfo
*.log
.env
.env.*
!.env.example
!.env.static

# NOTE: frontend/public/data/ is intentionally tracked. It is the curated
# radar snapshot published to GitHub Pages — regenerate it with
# `uv run radar build-static` and commit the change to publish. That commit
# is the deliberate "publish curated radar" gate (the private SQLite DB is
# never in CI). See docs/static-build.md.
